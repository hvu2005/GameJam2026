/// <summary>
/// Event types cho môi trường (Environment)
/// Sử dụng với EventBus
/// </summary>
public enum EnvironmentEventType
{
    EnterMud,  // Player vào bùn lầy
    ExitMud    // Player rời bùn lầy
}
